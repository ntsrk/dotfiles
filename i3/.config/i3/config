############### define Variables ###############
# set Super as mod key
set $mod Mod4


############### define fonts, border and gaps etc. ###############
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:JetBrainsMono Nerd Font 12
# font pango:DejaVu Sans Mono 10

# gaps between (inner) and around (outer) windows in px
gaps inner 12
gaps outer 1

# remove window border for focused window
for_window [class=".*"] border pixel 3
for_window [class="firefox"] border pixel 0
for_window [class="obsidian"] border pixel 0
for_window [class="spotify"] border pixel 0


# fonts, icons etc. for GTK3 can be changed with GUI lxappearance


############### MONITOR SETTINGS ###############
# choose setup by commenting and uncommenting
exec --no-startup-id autorandr --load parents
# exec_always --no-startup-id $HOME/.config/i3/parents-setup.sh
# exec_always --no-startup-id $HOME/.config/i3/university-setup.sh

# define which workspace goes on which screen
workspace $ws1 output HDMI-A-0
workspace $ws6 output eDP

# Disable screen goes black after inactivity
# see current screen saver functionality with "xset q" (timeout default was 600s)
# set timeout to 0 (deactivate)
exec xset s off
# deactivate power saving mode
exec xset -dpms


############### EXCECUTE SOME FILES ###############
exec_always --no-startup-id picom
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
# exec --no-startup-id nm-applet
exec --no-startup-id wal -R # damit die konfiguration von pywal wieder geladen werden


############### START SPECIFIC APPLICATIONS IN SPECIFIC WORKSPACES AT STARTUP ###############
# exec --no-startup-id i3-msg "workspace 1; exec firefox"
# exec --no-startup-id i3-msg 'workspace 3; exec /bin/bash /opt/Zotero_linux-x86_64/zotero -url'
# exec --no-startup-id i3-msg "workspace 5; exec spotify"
# exec --no-startup-id i3-msg "workspace 4; exec obsidian"


############### lockscreen, wallpaper ###############
# um ein neues Wallpaper zu setzten: wal -i <path/to/image> (das schreibt automatisch dann den neuen Pfad etc. in ~/.fehbg und generiert Colorpalette etc.
# merkt sich das letzte Bild das man gesetzt hat
# exec_always --no-startup-id /home/mdlxxiii/.fehbg
exec_always --no-startup-id sh -c "sleep 0.5 && /home/mdlxxiii/.fehbg" # benötigt kleine Verzögerung, da sonst das Timing nicht stimmt kein Plan
# change lockscreen wallpaper with "betterlockscreen -u <path/to/image> --display 1"


############### KEYBOARD SHORTCUTS TO START APPLICATIONS ###############
bindsym $mod+Return exec wezterm
# tool to make screenshots
bindsym $mod+Shift+p exec flameshot gui
bindsym $mod+space exec --no-startup-id rofi -show drun
bindsym $mod+c exec --no-startup-id "rofi -show calc -modi calc -no-show-match -no-sort > /dev/null"
bindsym $mod+b exec --no-startup-id "/home/mdlxxiii/Scripts/rofi-bash-launcher.sh" 
# bindsym $mod+Shift+space exec --no-startup-id rofi -show drun -run-shell-command '{terminal} -e bash -ic "{cmd} && read"'


############### KEYBOARD SHORTCUTS TO LOCKSCREEN, SUPSEND, SHUTDOWN, LOGOUT, RELOAD i3 ###############
bindsym $mod+Shift+b exec betterlockscreen -l --display 1
# with gui prompt to avoid accidentally suspending or shutting down pc
bindsym $mod+Shift+s exec --no-startup-id $HOME/.config/i3/suspend.sh
bindsym $mod+Shift+d exec --no-startup-id $HOME/.config/i3/shutdown.sh
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# reload i3 config file
bindsym $mod+Shift+c reload
# replace the entire i3 instance with a new one
bindsym $mod+Shift+r restart
# hibernate doesn't work idk why (maybe swap partition?) -> brauche ich eigentlich nicht -> suspend reicht völlig
# TODO bindsym $mod+Shift+h exec systemctl hibernate


############### CONTROL VOLUME WITH VOLUME KEYS ON KEYBOARD ###############
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume @DEFAULT_SINK@ +1%
bindsym XF86AudioLowerVolume exec pactl set-sink-volume @DEFAULT_SINK@ -1%
bindsym XF86AudioMute exec pactl set-sink-mute @DEFAULT_SINK@ toggle
# next, previous, pause and play with media keys on keyboard
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous


############### KEYBOARD SHORTCUTS FOR NAVIGATION, MOVING, RESIZING, CLOSING AND TILLING WINDOWS ###############
# keyboard shortcut to close the current focused application
bindsym $mod+q kill
# go to different windows in the current workspace
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
# move windows in the current workspace
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Use Mouse+$mod to drag floating windows to their wanted position on the current workspace
floating_modifier $mod
# tile horizontal or vertical
bindsym $mod+z split h
bindsym $mod+v split v
# toggle between fullscreen
bindsym $mod+f fullscreen

bindsym $mod+Shift+f floating toggle
# bindsym $mod+w layout tabbed
# bindsym $mod+q layout toggle split

# resize window (you can also use the mouse for that)
bindsym $mod+r mode "resize"

mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
}


############### KEYBOARD SHORTCUTS FOR WORKSPACES ###############
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10


